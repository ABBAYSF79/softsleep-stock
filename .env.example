# Optional. In local Vite, `/api` is proxied to localhost:3001 automatically.
# Only set this if you need to override (e.g. point at production while developing UI).
# VITE_API_URL=http://localhost:3001/api
